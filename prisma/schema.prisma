generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model User {
  id           String   @id @default(cuid())
  email        String   @unique
  passwordHash String
  isVerified   Boolean  @default(false)
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  profile  Profile?
  sessions Session[]
  refreshTokens RefreshToken[]
  emailVerificationToken EmailVerificationToken?
  bids Bid[]
  featured FeaturedAlumnus[]
  eventParticipations AlumniEventParticipation[]
  passwordResetTokens PasswordResetToken[]  
  apiClients ApiClient[]
  role String @default("USER")
}

model Profile {
  id        String   @id @default(cuid())
  userId    String   @unique
  fullName  String
  bio       String?
  linkedIn  String?
  imageUrl  String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  degrees Degree[]
  user User @relation(fields: [userId], references: [id], onDelete: Cascade)
  employmentHistory EmploymentHistory[]
  certifications Certification[]
  licences Licence[]
  courses Course[]
}

model Session {
  sid    String   @id
  sess   Json
  expire DateTime

  userId String?
  user   User? @relation(fields: [userId], references: [id], onDelete: SetNull)

  @@index([expire])
  @@index([userId])
}

model RefreshToken {
  id        String   @id @default(cuid())
  userId    String
  tokenHash String   @unique
  expiresAt DateTime
  revokedAt DateTime?
  createdAt DateTime @default(now())

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@index([expiresAt])
}


model EmailVerificationToken {
  id        String   @id @default(cuid())
  userId    String   @unique
  tokenHash String   @unique
  expiresAt DateTime
  usedAt    DateTime?

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([expiresAt])
}

model Degree {
  id          String   @id @default(cuid())
  profileId   String
  degreeName  String
  institution String
  year        Int
  createdAt   DateTime @default(now())

  profile Profile @relation(fields: [profileId], references: [id], onDelete: Cascade)

  @@index([profileId])
}

model EmploymentHistory {
  id          String   @id @default(cuid())
  profileId   String
  companyName String
  jobTitle    String
  startDate   DateTime
  endDate     DateTime?
  description String?
  createdAt   DateTime @default(now())

  profile Profile @relation(fields: [profileId], references: [id], onDelete: Cascade)

  @@index([profileId])
}

model Certification {
  id           String   @id @default(cuid())
  profileId    String
  name         String
  issuer       String
  year         Int
  createdAt    DateTime @default(now())

  profile Profile @relation(fields: [profileId], references: [id], onDelete: Cascade)

  @@index([profileId])
}

model Licence {
  id        String   @id @default(cuid())
  profileId String
  name      String
  issuer    String
  year      Int
  createdAt DateTime @default(now())

  profile Profile @relation(fields: [profileId], references: [id], onDelete: Cascade)

  @@index([profileId])
}

model Course {
  id        String   @id @default(cuid())
  profileId String
  name      String
  provider  String
  year      Int
  createdAt DateTime @default(now())

  profile Profile @relation(fields: [profileId], references: [id], onDelete: Cascade)

  @@index([profileId])
}


enum BidStatus{
  ACTIVE
  CANCELLED  
  WIN
  LOSE
  PENDING
}

model Bid {
  id        String   @id @default(cuid())
  userId    String
  amount    Float
  bidDate   DateTime
  status    BidStatus @default(ACTIVE)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, bidDate])
  @@index([bidDate])
}

model FeaturedAlumnus {
  id        String   @id @default(cuid())
  userId    String
  date      DateTime
  createdAt DateTime @default(now())

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([date])
}

model AlumniEventParticipation {
  id        String   @id @default(cuid())
  userId    String
  eventName String
  eventDate DateTime
  bonusUsed Boolean  @default(false)
  createdAt DateTime @default(now())

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId, eventDate])
}

model ApiClient {
  id        String        @id @default(cuid())
  name      String
  apiKey    String        @unique
  isActive  Boolean       @default(true)
  createdAt DateTime      @default(now())

  userId    String
  user      User          @relation(fields: [userId], references: [id], onDelete: Cascade)

  logs      ApiUsageLog[]
}

model ApiUsageLog {
  id          String    @id @default(cuid())
  apiClientId String
  endpoint    String?
  method      String?
  createdAt   DateTime  @default(now())

  apiClient   ApiClient @relation(fields: [apiClientId], references: [id], onDelete: Cascade)
}

model UsageLog {
  id        String   @id @default(cuid())
  userId    String?
  action    String
  endpoint  String?
  method    String?
  createdAt DateTime @default(now())
}

model PasswordResetToken {
  id        String   @id @default(cuid())
  userId    String
  tokenHash String   @unique
  expiresAt DateTime
  usedAt    DateTime?
  createdAt DateTime @default(now())

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)
}